<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoApp.Views.ToDoPage"
             NavigationPage.HasNavigationBar="False">

    <Grid ColumnDefinitions="1*,5*"> <!-- Left border auto width, right cards fill -->

        <!-- Left black border -->
        <Border Opacity="0.5"
                Grid.Column="0"
                BackgroundColor="Black"
                StrokeThickness="0"
                HorizontalOptions="Fill"
                VerticalOptions="Fill"/>

        <!-- Right side cards -->
        <ScrollView Grid.Column="1"> <!-- Scrollable if many cards -->
            <FlexLayout
                VerticalOptions="Start"
                Direction="Row"
                Wrap="Wrap"
                AlignItems="Start"
                JustifyContent="Start"
                Margin="10">

                <!-- Example card -->
                <Border
                    WidthRequest="300"
                    BackgroundColor="Black"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 12"
                    Opacity="0.8"
                    Margin="5,0,5,5"
                    VerticalOptions="Start">

                    <VerticalStackLayout Padding="15"
                                         Spacing="8"
                                         HorizontalOptions="Fill"
                                         VerticalOptions="Start">

                        <Label Text="To Do title" FontSize="26" FontAttributes="Bold" TextColor="White"/>
                        <Label Text="Some description text" TextColor="White" FontSize="16"/>

                        <HorizontalStackLayout Spacing="10" HorizontalOptions="Fill">
                            <Button Text="Done" HorizontalOptions="EndAndExpand" BackgroundColor="LimeGreen" TextColor="White" FontSize="20" WidthRequest="120"/>
                            <Button Text="Delete" HorizontalOptions="StartAndExpand" BackgroundColor="Red" TextColor="White" FontSize="20" WidthRequest="120"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Border>
                
            </FlexLayout>
        </ScrollView>
    </Grid>
</ContentPage>
